import { defineConfig } from 'vite'

export default defineConfig({
  // If your repository name is different from the username,
  // you'll need to set the base path to match your repo name
  // For example: '/repo-name/'
  base: '/',
})
